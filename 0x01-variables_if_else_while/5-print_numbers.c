#include <stdio.h>
/**
 * main - main block
 * Description: this print base 10 numbers
 * Return: 0
 */
int main(void)
{
	int n = 0;

	while (n < 10)
	{
		printf("%d", n);
		n++;
	}
	printf(\n);
	return (0);
}
